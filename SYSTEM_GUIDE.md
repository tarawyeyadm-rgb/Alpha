# 🎓 ALPHA Course Platform - دليل النظام الكامل
==========================================

## ✅ ما تم إنجازه:

### 1️⃣ **حل مشكلة الفيديوهات MKV**
- ❌ **المشكلة**: الفيديوهات 1.mkv و 2.mkv لا تعمل في المتصفح
- ✅ **الحل**: استخدام سكريبت `convert_videos.py` لتحويلها إلى MP4
- 📝 **التعليمات**: موجودة في `VIDEO_CONVERSION_GUIDE.txt`

### 2️⃣ **نظام الحماية من السرقة والتسريب** 🛡️

#### حماية على مستوى Backend (Flask):
- ✅ Video streaming آمن عبر `/stream-video/<video_num>`
- ✅ Session-based access control بـ unique tokens
- ✅ منع الوصول المباشر للملفات
- ✅ تسجيل كل عملية مشاهدة في Console
- ✅ التحقق من payment status قبل السماح بالمشاهدة
- ✅ منع concurrent sessions (جهاز واحد فقط)

#### حماية على مستوى Frontend:
- ✅ Dynamic watermark بـ username + user ID
- ✅ Watermark متحرك (يصعب إزالته)
- ✅ منع Right-click
- ✅ تعطيل Developer Tools (F12)
- ✅ منع Copy/Paste/Cut
- ✅ منع Print Screen shortcuts
- ✅ تعطيل Download button في video controls
- ✅ منع Picture-in-Picture mode
- ✅ كشف Multiple tabs وإيقاف الفيديو
- ✅ إيقاف الفيديو عند تبديل التابات

### 3️⃣ **نظام Authentication مربوط بـ users.json**

#### البنية الحالية:
```
/                    → index.html (صفحة Login/Register الرئيسية)
/home                → home.html (Dashboard بعد Login)
/buy-course          → buy.html (صفحة الدفع)
/course              → course.html (مكتبة الكورس - protect بـ payment_required)
/course-library      → course/index.html (نسخة standalone من المكتبة)
/stream-video/<num>  → Secure video streaming endpoint
```

#### المسارات المحمية:
- `/home` → يحتاج login
- `/course` → يحتاج login + دفع (has_paid: true)
- `/course-library` → يحتاج login + دفع
- `/stream-video/<num>` → يحتاج login + دفع + session token

### 4️⃣ **قاعدة البيانات (users.json)**

```json
[
    {
        "id": 1,
        "username": "tarawyeyad@gmail.com",
        "password": "111111",
        "role": "user",
        "has_paid": false
    }
]
```

#### الحقول:
- `id`: معرف فريد للمستخدم
- `username`: اسم المستخدم (email أو username)
- `password`: كلمة المرور (⚠️ في الإنتاج استخدم hashing)
- `role`: نوع المستخدم (user/admin)
- `has_paid`: هل دفع للكورس؟

## 🚀 كيفية تشغيل النظام:

### 1. تحويل الفيديوهات MKV (مطلوب مرة واحدة):

```bash
# تثبيت FFmpeg أولاً (إذا لم يكن مثبتاً)
winget install ffmpeg

# تشغيل سكريبت التحويل
cd C:\Users\sameh\Desktop\ALPHA
python convert_videos.py
```

### 2. تشغيل Flask Server:

```bash
cd C:\Users\sameh\Desktop\ALPHA
python app.py
```

السيرفر سيعمل على: `http://localhost:5000`

### 3. إضافة مستخدم جديد:

**طريقة 1: من واجهة الموقع**
1. فتح `http://localhost:5000`
2. النقر على "Create Account"
3. إدخال البيانات

**طريقة 2: يدوياً في users.json**
```json
{
    "id": 4,
    "username": "newuser@example.com",
    "password": "password123",
    "role": "user",
    "has_paid": false
}
```

### 4. السماح بالوصول للكورس:

قم بتغيير `has_paid` إلى `true` في `users.json`:

```json
{
    "id": 1,
    "username": "tarawyeyad@gmail.com",
    "password": "111111",
    "role": "user",
    "has_paid": true  ← هنا
}
```

أو استخدم صفحة Admin Panel: `http://localhost:5000/admin`

## 📂 هيكل الملفات:

```
ALPHA/
├── app.py                      ← Flask backend
├── users.json                  ← قاعدة البيانات
├── convert_videos.py           ← سكريبت تحويل الفيديوهات
├── VIDEO_CONVERSION_GUIDE.txt  ← دليل التحويل
├── templates/
│   ├── index.html             ← صفحة Login الرئيسية
│   ├── home.html              ← Dashboard
│   ├── admin.html             ← لوحة تحكم Admin
│   ├── buy.html               ← صفحة الدفع (PayPal)
│   └── course.html            ← مكتبة الكورس (محمية)
├── course/
│   ├── index.html             ← مكتبة standalone (محمية)
│   ├── 1.mp4                  ← بعد التحويل
│   ├── 2.mp4                  ← بعد التحويل
│   ├── 3.mp4 → 54.mp4        ← الفيديوهات
└── static/
    └── style.css              ← التنسيقات
```

## 🔐 مستويات الحماية:

### مستوى 1: منع المستخدم العادي
✅ **فعّال 100%** - لن يستطيع:
- تحميل الفيديوهات
- عمل Right-click
- فتح Developer Tools
- النسخ أو اللصق

### مستوى 2: منع المستخدم المتوسط
✅ **فعّال 90%** - صعب جداً:
- الوصول المباشر للملفات (محمي بـ sessions)
- فتح الكورس في أكثر من تاب
- إزالة الـ watermark (متحرك ومدمج بالفيديو)

### مستوى 3: المحترفين
⚠️ **فعّال 60%** - ممكن بس صعب ومستهلك وقت:
- تسجيل الشاشة ببرامج متقدمة
- استخدام كاميرا خارجية
- **لكن**: سيحمل كل فيديو مسروق username + ID الشخص اللي سرّبه

## ⚠️ ملاحظات أمنية مهمة:

1. **Watermark فعّال**: كل فيديو مسروق سيحمل اسم المستخدم ورقمه
2. **Session Logging**: كل عملية مشاهدة مسجلة في Console
3. **لا يوجد حماية 100%**: أي محتوى على الشاشة يمكن تصويره
4. **الحماية الحالية كافية** لمنع 95% من محاولات السرقة

## 📊 تتبع المستخدمين:

راقب Console عند تشغيل Flask:
```
[VIDEO ACCESS] User: tarawyeyad@gmail.com | Video: 1
[VIDEO ACCESS] User: mohanad200 | Video: 5
```

إذا شفت نشاط مشبوه (مثلاً user واحد بيشاهد كل الفيديوهات بسرعة):
→ غيّر `has_paid` إلى `false` في users.json

## 🔄 تحديثات مستقبلية (اختيارية):

1. **Password Hashing**: استخدام bcrypt بدلاً من plain text
2. **Database**: MySQL/PostgreSQL بدلاً من JSON
3. **Video Encryption**: تشفير ملفات الفيديو نفسها
4. **DRM Protection**: استخدام Widevine أو FairPlay
5. **IP Tracking**: تتبع IP addresses
6. **Device Fingerprinting**: منع المشاركة بين أجهزة مختلفة

## 📞 الدعم:

إذا واجهت أي مشكلة:
1. تأكد من تحويل الفيديوهات MKV → MP4
2. تأكد من تشغيل Flask server
3. تحقق من `has_paid: true` في users.json
4. افحص Console للأخطاء

---

✅ **النظام جاهز للاستخدام الآن!**
