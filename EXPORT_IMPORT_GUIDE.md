# User Management - Export & Import Feature Guide
# دليل استخدام ميزة التصدير والاستيراد لإدارة المستخدمين

## ✅ التعديلات المنفذة بنجاح:

### 1. واجهة المستخدم (admin.html)
- ✅ إضافة زر "📥 Export Users" لتحميل ملف users.json
- ✅ إضافة زر "📤 Import Users" لاستيراد ملف users.json  
- ✅ تصميم جذاب ومتناسق مع باقي الواجهة

### 2. البرمجة الخلفية (app.py)
- ✅ إضافة endpoint: `/api/users/export` (GET)
  - يصدر جميع المستخدمين في ملف JSON منسق
  - يتطلب صلاحيات admin
  - اسم الملف: `users.json`

- ✅ إضافة endpoint: `/api/users/import` (POST)
  - يستورد مستخدمين من ملف JSON
  - يتحقق من صحة البيانات
  - يتجنب التكرار (يتخطى المستخدمين الموجودين)
  - يعيد تقرير بعدد المستخدمين المضافين والمتخطين
  - يتطلب صلاحيات admin

### 3. البرمجة الأمامية (script.js)
- ✅ وظيفة `exportUsers()`:
  - تستدعي API endpoint
  - تنزل الملف تلقائياً بتسمية مع التاريخ
  - تعرض رسالة نجاح
  
- ✅ وظيفة `importUsers(event)`:
  - تتحقق من نوع الملف (.json فقط)
  - تتحقق من صحة البنية (array of objects)
  - تتحقق من وجود الحقول المطلوبة (username, password)
  - تعرض رسالة تأكيد قبل الاستيراد
  - تعرض نتيجة العملية (عدد المضاف/المتخطى)

### 4. التنسيقات (style.css)
- ✅ إضافة class: `.btn-secondary`
- ✅ تأثيرات hover جذابة
- ✅ تناسق مع التصميم العام

---

## 📖 كيفية الاستخدام:

### 1️⃣ تصدير المستخدمين (Export):
1. سجل دخول كـ Admin
2. اذهب إلى صفحة User Management (`/admin`)
3. اضغط على زر "📥 Export Users"
4. سيتم تنزيل ملف `users.json` تلقائياً
5. الملف سيحتوي على جميع بيانات المستخدمين بصيغة JSON منسقة

### 2️⃣ استيراد المستخدمين (Import):
1. سجل دخول كـ Admin
2. اذهب إلى صفحة User Management (`/admin`)
3. اضغط على زر "📤 Import Users"
4. اختر ملف `.json` من جهازك
5. سيظهر رسالة تأكيد بعدد المستخدمين
6. اضغط OK للتأكيد
7. سيتم إضافة المستخدمين الجدد وتخطي الموجودين
8. ستظهر رسالة بالنتيجة (عدد المضاف/المتخطى)

---

## 🔒 الأمان:
- ✅ كلا الـ endpoints محمية بـ `@admin_required` decorator
- ✅ التحقق من صحة البيانات قبل الإضافة
- ✅ لا يتم الكتابة فوق المستخدمين الموجودين
- ✅ معالجة الأخطاء بشكل صحيح

---

## 📁 مثال على ملف users.json:

```json
[
    {
        "id": 1,
        "username": "Eyad",
        "password": "eyad123321@@##$$",
        "role": "admin",
        "has_paid": true
    },
    {
        "id": 2,
        "username": "salah",
        "password": "salah00913495@#9846",
        "role": "user",
        "has_paid": true
    }
]
```

---

## 🌐 للوصول للتطبيق:
- المحلي: http://127.0.0.1:5000
- الشبكة: http://192.168.8.124:5000

### حسابات Admin للاختبار:
1. Username: `admin` | Password: `admin_password_123`
2. Username: `Eyad` | Password: `eyad123321@@##$$`
3. Username: `mohanad200` | Password: `Mohanad2009@`

---

## ✨ المميزات:
- 🎨 تصميم جذاب مع الأيقونات
- 🔒 آمن تماماً (Admin فقط)
- ✅ التحقق من صحة البيانات
- 📊 رسائل واضحة للمستخدم
- 🚀 سريع وفعال
- 💾 نسخ احتياطي تلقائي مع التاريخ

---

## 🔍 اختبار سريع:
1. شغل التطبيق: `python app.py`
2. افتح المتصفح: http://127.0.0.1:5000
3. سجل دخول كـ admin
4. جرب Export لتحميل المستخدمين الحاليين
5. عدل الملف يدوياً (أضف مستخدم جديد)
6. جرب Import لاستيراد التعديلات

تم التنفيذ بنجاح ✅
