================================================================================
📜 تقرير مشروع منصة ALPHA التعليمية (Advanced Security Course Platform)
================================================================================

تاريخ التقرير: 2026-01-20
اسم المشروع: ALPHA Project
الهدف: توفير منصة آمنة لبيع وعرض دورات تدريبية رقمية مع حماية قصوى من التسريبات.

--------------------------------------------------------------------------------
1. التقنيات المستخدمة (Technical Stack)
--------------------------------------------------------------------------------
- لغة البرمجة: Python
- الإطار البرمجي: Flask (Backend Web Framework)
- قاعدة البيانات: JSON (`users.json`) - نظام خفيف وسريع لتخزين بيانات المستخدمين.
- الواجهة الأمامية (Frontend): HTML5, CSS3 (Vanilla + Glassmorphism Design), JavaScript.
- الحزمة المساعدة: Requests (للتعامل مع بوابات الدفع).

--------------------------------------------------------------------------------
2. المميزات الرئيسية (Core Features)
--------------------------------------------------------------------------------
أ- نظام المستخدمين:
   - تسجيل حساب جديد وتسجيل الدخول.
   - إدارة صلاحيات المستخدمين (Has Paid flag) عبر الرابط مع قاعدة البيانات.

ب- مكتبة الفيديوهات:
   - عرض 54 درساً تعليمياً منظماً.
   - نظام تشغيل فيديوهات ذكي يدعم مختلف الصيغ (MP4, MKV).

ج- نظام الدفع (NOWPayments):
   - الدفع حصرياً بالعملات الرقمية (Crypto-only) لضمان الخصوصية.
   - السعر الحالي: 50 دولار أمريكي.
   - العملات المدعومة: Bitcoin, Ethereum, USDT (TRC20), Litecoin وغيرها.
   - تفعيل تلقائي للكورس فور تأكيد الدفع عبر نظام الـ Webhooks.

--------------------------------------------------------------------------------
3. حماية المحتوى ومكافحة القرصنة (Anti-Piracy & Security)
--------------------------------------------------------------------------------
هذا المشروع مزود بأقوى وسائل الحماية لمنع سرقة أو تسريب المحتوى:
- العلامة المائية الديناميكية (Dynamic Watermark): يظهر اسم المستخدم ورقم تعريفه (ID) فوق الفيديو بشكل متحرك، مما يجعل التعرف على المسرب أمراً سهلاً.
- منع التحميل المباشر: الفيديوهات يتم بثها عبر مسارات مشفرة (Streaming) وليس عبر روابط مباشرة للملفات.
- حماية الواجهة: تعطيل الزر الأيمن للفأرة، منع اختصارات لوحة المفاتيح (F12, Ctrl+U, Ctrl+S)، ومنع تصوير الشاشة في المتصفحات المدعومة.
- كشف التلاعب: نظام لمراقبة أدوات المطورين (DevTools) ومنع فتح الكورس في أكثر من علامة تبويب (Tab) في نفس الوقت.

--------------------------------------------------------------------------------
4. هيكل الملفات (File Structure)
--------------------------------------------------------------------------------
- `app.py`: القلب النابض للمشروع (الباك-إند).
- `users.json`: قاعدة بيانات المستخدمين وكلمات مرورهم وحالة الدفع.
- `course/`: المجلد الذي يحتوي على ملفات الفيديو الأصلية وصفحة الفهرس.
- `templates/`: ملفات الواجهة (Home, Login, Course, Crypto Payment).
- `convert_videos.py`: أداة برمجية أضفناها لتحويل الفيديوهات من صيغة MKV الواسعة الانتشار إلى MP4 المتوافقة مع جميع المتصفحات.

--------------------------------------------------------------------------------
5. كيفية الإدارة والتشغيل
--------------------------------------------------------------------------------
- للبدء: تشغيل الأمر `python app.py` وفتح المتصفح على `localhost:5000`.
- لتفعيل حساب يدوياً: تغيير قيمة `"has_paid": true` في ملف `users.json` أمام المستخدم المطلوب.
- لمراقبة المبيعات: يمكن متابعة الـ Console الخاص بـ Flask لرؤية الـ Webhooks القادمة من NOWPayments.

================================================================================
✅ تم إعداد التقرير بنجاح - تم تصميم هذا المشروع ليكون جاهزاً للنشر (Production Ready) 
مع التركيز التام على تجربة المستخدم (UX) وأمان المحتوى.
================================================================================
